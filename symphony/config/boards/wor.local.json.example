{
  "// usage": "Copy this file to wor.local.json (gitignored). Add project entries for Linear projects not yet in wor.json.",
  "// merge rules": "projects[] upserted by linearProjectId, repos[] upserted by name. Scalar fields replace base values.",
  "// tip": "Run the sync-projects skill to auto-generate entries from your Linear projects.",

  "projects": [
    {
      "linearProjectId": "07d6f5ab-3ee8-4bd8-9ae6-e0156db7fb1a",
      "name": "ws-components",
      "primaryRepo": "workstream-mono",
      "repos": [
        {
          "name": "workstream-mono",
          "path": "~/Documents/workstream-mono/libs/ws-components",
          "hint": "Shared UI component library used across all MFE sub-apps"
        }
      ]
    }
  ]
}
